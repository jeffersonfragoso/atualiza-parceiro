version: "3.8"

services:
  api:
    container_name: api
    env_file:
      - ./.env
    build: ./
    image: atualiza-parceiro-image
    volumes:
      - ./:/home/atualiza-parceiro/
    ports:
      - 8000:8000
    command: >
      python -m
        uvicorn
        --factory
        entrypoints.web.main:create_app
        --loop 'uvloop'
        --http 'httptools'
        --log-level debug
        --reload
        --workers 1
        --host 0.0.0.0
        --port 8000
